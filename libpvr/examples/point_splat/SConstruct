# Alter these as needed

program    = "point_splat"
srcDir     = "src"
pathToRoot = "../.."

# Standard script below

import sys
sys.path.append(pathToRoot)
from BuildSupport import *

env = Environment()
makeSimpleProgram(env, pathToRoot, program, srcDir)
